<div class=" col-md-offset-2 col-xs-12 col-sm-8 col-md-8">
  <a class="margin btn btn-primary" href="http://localhost:3000/cocktails/new" role="button">Ajouter une biere</a>
</div>
<div class="col-md-offset-2 col-xs-12 col-sm-8 col-md-8">
  <div class="white_bg opacity">    <h1><%= @cocktail.name %></h1>

    <% @cocktail.doses.each do |dose|%>
    <%= (Ingredient.find(dose.ingredient_id)).name %>
    <%= dose.description %>

    <%end%><br>

    <%= link_to "Back to Index", cocktails_path %> <br>
    <%= link_to "Add dose", new_cocktail_dose_path(@cocktail) %> <br>

</div>
